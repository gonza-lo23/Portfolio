
import { extendTheme, theme } from '@chakra-ui/react';

export default extendTheme({
    config: {
        initialColorMode: "light",
    },
    
    colors: {
        primary: 'tomato',
        second:'white'
    },
});